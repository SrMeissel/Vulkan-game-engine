using GameEngine;
using System;

class Rotate : EngineCore
{
    public void update()
    {
        //Object.transform.rotation.X += deltaTime / 2;
        //Object.transform.rotation.Y += deltaTime / 2;
        //Object.transform.rotation.Z += deltaTime / 2;

        //Console.WriteLine(Object.transform.rotation.ToString());
    }
}
